<%-include("../layout/head")%>
<%-include("../layout/header")%>
<%-include("../layout/sidebar")%>

<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
    <div class="row">
        <ol class="breadcrumb mt-3">
            <li>
                <a href="/admin">
                    <svg class="glyph stroked home">
                        <use xlink:href="#stroked-home"></use>
                    </svg>
                </a>
            </li>
            <li><a href="/admin/banners<%= type ? ('?type=' + type) : '' %>">Quản lý quảng cáo</a></li>
            <li class="active">Thêm quảng cáo</li>
        </ol>
    </div>

    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
    <div class="alert alert-danger mt-3">
        <ul>
            <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
            <% }) %>
        </ul>
    </div>
    <% } %>

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Thêm quảng cáo</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <form method="POST" action="<%= (type === 'slider') ? '/admin/sliders/store' : '/admin/banners/store' %>"
                enctype="multipart/form-data">
                <div class="panel panel-default">
                    <div class="panel-body">
                        <table class="table table-bordered">
                            <tr>
                                <th style="width:200px;">Ảnh <span
                                        style="color:red;">*</span></th>
                                <td>
                                    <input type="file" name="image"
                                        class="form-control" accept="image/*"
                                        required>
                                    <small class="text-muted">Chỉ chấp nhận định
                                        dạng ảnh (jpg, png, gif)</small>
                                </td>
                            </tr>
                            <tr>
                                <th>Link (URL)</th>
                                <td>
                                    <input type="url" name="url"
                                        class="form-control"
                                        placeholder="https://example.com"
                                        value="<%= oldData.url || '' %>">
                                    <input type="hidden" name="type" value="<%= type %>">
                                </td>
                            </tr>
                            <tr>
                                <th>Mở tab mới</th>
                                <td>
                                    <select name="target" class="form-control">
                                        <option value="false" <%= !oldData.target || oldData.target === 'false' ? 'selected' : '' %>>Không</option>
                                        <option value="true" <%= oldData.target === true || oldData.target === 'true' ? 'selected' : '' %>>Có</option>
                                    </select>
                                </td>
                            </tr>
                            <tr>
                                <th>Vị trí</th>
                                <td>
                                    <input type="text" class="form-control" 
                                        value="Đầu tiên (tự động)" disabled>
                                    <small class="text-muted">Banner mới sẽ tự động được đặt ở vị trí đầu tiên. Các banner cũ sẽ tự động xuống dưới.</small>
                                </td>
                            </tr>
                            <tr>
                                <th>Trạng thái</th>
                                <td>
                                    <select name="publish" class="form-control"
                                        required>
                                        <option value="false"
                                            <%=(oldData.publish === false ||
                                            oldData.publish === 'false') ?
                                            'selected' : '' %>>Ẩn</option>

                                        <option value="true" <%=(oldData.publish
                                            === true || oldData.publish ===
                                            'true' || oldData.publish ===
                                            undefined) ? 'selected' : '' %>>Hiển
                                            thị</option>
                                    </select>
                                </td>
                            </tr>
                        </table>
                    </div>
                    <div class="panel-footer"
                        style="display:flex; justify-content:space-between; align-items:center;">
                        <div>
                            <a href="/admin/banners<%= type ? ('?type=' + type) : '' %>" class="btn btn-default">
                                <i class="glyphicon glyphicon-arrow-left"></i>
                                Quay lại
                            </a>
                        </div>
                        <div>
                            <button type="submit" class="btn btn-success">
                                <i class="glyphicon glyphicon-floppy-disk"></i>
                                Lưu
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="/static/admin/js/jquery-1.11.1.min.js"></script>
<script src="/static/admin/js/bootstrap.min.js"></script>
<%-include("../layout/footer")%>